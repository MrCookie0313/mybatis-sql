mybatis:
  mapper-locations: classpath:mappers/*xml
  type-aliases-package: com.cookie.demo.mybatis.entity
server:
  port: 8080
spring:
  application:
    name: show-mybatis
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    password: root
    url: jdbc:mysql://localhost:3306/xxl-job?serverTimezone=UTC
    username: root
